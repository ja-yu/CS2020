# computer netwokr計算機網路   網路概論
```
說明OSI Model與 TCP/IP protocol
```
```

```
# 協定protocol
```
何謂communcation protocol[簡稱協定protocol]?
WHY Layering為何要分層?
```


## 各種協定
```
簡述下列協定protocol
須回答
(A)英文全名
(B)運作在那一層?
(C)主要功能
(D)相關工具或伺服器
```
```
HTTP
1.HTTP英文全名為HyperText Transfer Protocol
2.HTTP運作在Application layer
3.HTTP是用戶端和伺服器端之間溝通的標準


HTTPS
1.HTTPS英文全名為HyperText Transfer Protocol Secure


FTP
1.FTP英文全名為File Transfer Protocol


SFTP 
1.SFTP英文全名為SSH File Transfer Protocol 或 Secure File Tansfer Protocol


FTPS
1.FTPS英文全名為File Transfer Protocol SSL 或 File Transfer Protocol Secure


TELNET 
1.TELNET英文全名為teletype network


SSH
1.SSH英文全名為Secure Shell


SMTP 
1.SMTP英文全名為Simple Mail Transfer Protocol


POP3 
1.POP3英文全名為Post Office Protocol - Version 3


IMAP4
1.IMAP4英文全名為Internet Message Access Protocol - Version 4

DNS 
1.DNS英文全名為Domain Name System


DNSsec
1.DNSSEC英文全名為Domain Name System Security Extensions


DHCP
1.DHCP英文全名為Dynamic Host Configuration Protocol


LDAP
1.LDAP英文全名為Lightweight Directory Access Protocol


SAMBA
1.


SNMP
1.SNMP英文全名為Simple Network Management Protocol


RTMP 
1.RTMP英文全名為Real-Time Messaging Protocol


RTSP
1.RTSP英文全名為Real Time Streaming Protocol


MQTT
1.MQTT英文全名為Message Queuing Telemetry Transport


```
```
HTTP
超文本傳輸協定（HyperText Transfer Protocol)
應用層（Application layer）位於OSI模型的第七層
HTTP是一個客戶端（用戶）和伺服器端（網站）之間請求和應答的標準

HTTP客戶端發起一個請求，建立一個到伺服器指定埠（初始為80埠）的TCP連線。HTTP伺服器則在那個埠監聽客戶端的請求

超文本傳輸安全協定（Hypertext Transfer Protocol Secure, 常稱為HTTP over TLS、HTTP over SSL或HTTP Secure）
HTTPS經由HTTP進行通訊，但利用SSL/TLS來加密封包
是提供對網站伺服器的身分認證，保護交換資料的隱私與完整性

檔案傳輸協定（File Transfer Protocol）是一個用於在電腦網路上在客戶端和伺服器之間進行檔案傳輸的應用層協定

SSH檔案傳輸協定（SSH File Transfer Protocol，也稱Secret File Transfer Protocol，中文：安全檔案傳送協定，英文：Secure FTP或字母縮寫：SFTP）是一數據流（Data stream）連線，提供檔案存取、傳輸和管理功能的網路傳輸協定
可以為傳輸文件提供一種安全的加密方法

FTPS是一種對常用的檔案傳輸協定（FTP）添加傳輸層安全（TLS）和安全通訊協定（SSL）加密協定支援的擴充協定
加密版的FTP

Telnet是一種應用層協定，使用於網際網路及區域網中，使用虛擬終端機的形式，提供雙向、以文字字串為主的命令列介面互動功能
常用於伺服器的遠端控制，可供使用者在本地主機執行遠端主機上的工作

Secure Shell（安全外殼協定，簡稱SSH）是一種加密的網路傳輸協定，可在不安全的網路中為網路服務提供安全的傳輸環境

傳統Telnet會話所傳輸的資料並未加密，帳號和密碼等敏感資料容易會被竊聽，因此很多伺服器都會封鎖Telnet服務，改用更安全的SSH

簡單郵件傳輸協定（Simple Mail Transfer Protocol，縮寫：SMTP）是一個在網際網路上傳輸電子郵件的標準。
SMTP是一個相對簡單的基於文字的協定

郵局協議（英語：Post Office Protocol，縮寫：POP）此協定主要用於支援使用客戶端遠端管理在伺服器上的電子郵件。最新版本為POP3，全名「Post Office Protocol - Version 3」，而提供了SSL加密的POP3協定被稱為POP3S

網際網路資訊存取協定（英語：Internet Message Access Protocol，縮寫：IMAP；以前稱作互動郵件存取協定）是一個應用層協定，用來從本地郵件客戶端（如Microsoft Outlook、Outlook Express、Foxmail、Mozilla Thunderbird）存取遠端伺服器上的郵件

SMTP是一個「推播「協定，它不允許根據需要從遠端伺服器上「拉取」訊息。要拉取訊息到客戶端，郵件客戶端必須使用POP3或IMAP。
```

### application層
```
HTTP vs HTTPS
FTP vs SFTP | FTPS
TELNET vs SSH
SMTP vs POP3 INAP 4

DNS vs DNSsec

DHCP

LDAP
SAMBA

SNMP

RTMP RTSP

MQTT
```
### transport層
```
TCP 與UDP的差異
[1]須說明reliable(可靠) vs un-reliable(不可靠)
[2]如何達到reliable(可靠)
 (A)錯誤重傳
 (B) ThreeWay Handshaking
[3]封包格式 TCP format   vs UDP format 
```
### 
```
IP vs IPsec
ICMP
```
### 連結層
```
ARP
```
# 網路硬體設備 Network Devices
```
簡述底下網路設備:須說明
(A)運作在OSI那一層?
(B)主要功能


(1) HUB
(2) Switch
(3) Router
(4) L4 Switch
(5) Proxy 
```
# 各種address位址
```
PORT address
IP address
MAC address
```
